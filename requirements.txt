gunicorn==23.0.0
python-dateutil==2.9.0.post0
Flask==3.1.0
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.10
SQLAlchemy==2.0.36
python-dotenv==1.0.1
openpyxl==3.1.5
email-validator==2.2.0

# ── API Gateway (Phase 4) ──
fastapi==0.104.1
uvicorn[standard]==0.24.0
# Phase 1/2 dependencies imported by the gateway at module load
routeros_api==0.21.0
pysnmp==7.1.28
# ── Turso (hosted libSQL) — cloud database for Render ──
# database.py connects to Turso via its SQL-over-HTTP endpoint using Python
# STDLIB only (urllib/json) — NO third-party package is required. We
# deliberately avoid libsql-experimental because it ships as a Rust extension
# without Windows wheels and fails to install (would break the Render build).
# The local desktop EXE stays on the local SQLite file.

