# 🗺️ AL-NATHIM MOBILE APP — FULL PHASED PLAN (بمنهجية Blood)

> **المبدأ الحاكم:** كل مرحلة تنتهي بتسليم قابل للفحص. بعد انتهاء المرحلة «N» تعطيني موافقتك (Access) — وعندها فقط أنتقل للمرحلة «N+1». لا أبدأ مرحلة جديدة قبل موافقتك الصريحة.

---

## 📌 النظرة العامة (Vision)

| العنصر | الوصف |
|---|---|
| **الهدف** | تطبيق أندرويد سريع وحلو (مثل استايل Blood) لإدارة شبكة الإنترنت |
| **الخلفية (Backend)** | السيرفر (Flask + Render) — هو مصدر الحقيقة لكل البيانات |
| **قسم الأجهزة (Devices)** | يعمل مثل الـ EXE المحلي: Ping + Sector + SNMP مباشرة من التلفون على شبكة البرج |
| **الاستراتيجية** | إكمال `android_native/` الموجود (Kotlin + Compose) — لا نبدأ من الصفر |
| **التسليم النهائي** | APK جاهز للتركيب + ويب متطابق + خلفية موثوقة |

---

## 🏗️ البنية المستهدفة (Target Architecture)

```
┌──────────────────────────────────────────────┐
│  تطبيق الأندرويد (android_native/)           │
│  ┌───────────────┐   ┌────────────────────┐  │
│  │  شاشات Compose│   │ الجسر المحلي        │  │
│  │  (عربية RTL)  │   │ NativeBridge       │  │
│  └───────┬───────┘   │ Ping/SNMP/RouterOS │  │
│          │           └────────────────────┘  │
│          ▼                                    │
│  ┌───────────────────────────────────────┐    │
│  │  Retrofit + MobileApiService          │    │
│  │  (Bearer token /api/mobile/v1)        │    │
│  └───────────────────┬───────────────────┘    │
└──────────────────────┼────────────────────────┘
                       ▼ HTTPS
┌──────────────────────────────────────────────┐
│  الخلفية — Flask على Render                  │
│  app.py + database.py + mobile_api.py        │
│  SQLite / Turso (مصدر الحقيقة)               │
│  + snmp_monitor + network_tools + mikrotik   │
└──────────────────────────────────────────────┘
```

---

## 📂 الملفات المعنية (Files to Modify)

| الملف | المسار | العمل |
|---|---|---|
| شاشة الشبكة | `android_native/.../ui/networktools/NetworkToolsScreen.kt` | إعادة تصميم لتبويبات Ping/سكتورات/SNMP |
| ViewModel الشبكة | `android_native/.../ui/networktools/NetworkToolsViewModel.kt` | دعم SNMP tab والإدخالات |
| API Service | `android_native/.../data/network/MobileApiService.kt` | تأكيد/إضافة الـ endpoints |
| Repository | `android_native/.../data/network/NetworkRepository.kt` | استدعاء الـ endpoints |
| Native probe | `android_native/.../data/network/probe/*.kt` | موجود (DirectPing/RouterOS/Snmp) |
| Web UI | `templates/network.html` | مزامنة سلوك الـ EXE |
| Backend API | `mobile_api.py` | إضافة أي endpoint ناقص |
| API Contract | `API_CONTRACT.md` | تحديث التوثيق |
| Build | `build_android.bat` | موجود ✅ |

---

# 🚦 المراحل (PHASES)

---

## المرحلة 0 — التحقق والتحضير (صفر شفرة)
**الهدف:** التأكد أن البناء يعمل قبل أي تعديل.
- [ ] تشغيل `build_android.bat` → التأكد أن `app-debug.apk` يبني بنجاح.
- [ ] فتح المشروع والتحقق من بنية الملفات الأساسية.
- [ ] تجهيز قائمة نهائية بالملفات التي سنعدلها.

**التسليم:** 📦 بناء APK أخضر + قائمة ملفات معتمدة.

**البوابة:** تفحص النتيجة → تعطيني موافقة → المرحلة 1.

---

## المرحلة 1 — قسم الأجهزة: شاشة الشبكة مثل الـ EXE
**الهدف:** شاشة أجهزة كاملة من التلفون تعمل محلياً (مثل الـ EXE).

### 1.1 تبويب Ping
- حقل عنوان (IP/نطاق) + عدد حزم (1–10) + أزرار اختصارات (Google/YouTube/Facebook/TikTok/1.1.1.1).
- زر فحص → **فحص ICMP مباشر من التلفون** عبر `NativeBridge.pingHost`.
- عرض: متوسط / أقل / أعلى / فقدان (٪) + عدّاد حزم مرسلة/مستلمة.
- fallback تلقائي لـ TCP probe (80/443) إذا كان ICMP محجوب.

### 1.2 تبويب السيكتورات
- قائمة سكتورات (اسم، نوع، IP، موقع، حالة).
- إضافة/تعديل/حذف سكتور (اسم، نوع، IP، موقع، يوزر/باسورد، SNMP community).
- زر **«فحص مباشر»** لكل سكتور → يفحص من التلفون: منافذ MikroTik (8728/22/8291) + RouterOS /ppp/active + SNMP signal/CCQ.
- عرض النتائج: متصل/غير متصل، منافذ مفتوحة، زمن الاستجابة، مشتركين نشطين، إشارة dBm، CCQ%.

### 1.3 تبويب SNMP
- حقل IP + نوع الجهاز (تلقائي/نانو واي فاي/كيبل ضوئي GPON) + community.
- زر فحص → **قراءة SNMP مباشرة من التلفون** عبر `SnmpClient`.
- عرض: RX dBm / TX dBm / الإشارة dBm / CCQ%.

### 1.4 مزامنة الويب
- تعديل `templates/network.html` ليطابق نفس السلوك (عندما لا يكون داخل التطبيق يمر بالسيرفر).

**التسليم:** 📦 قسم أجهزة يعمل من التلفون (Ping+Sector+SNMP) + الويب متطابق.

**البوابة:** تجرب الفحص من تلفونك → موافقة → المرحلة 2.

---

## المرحلة 2 — ربط الخلفية بالسيرفر (البيانات)
**الهدف:** كل بيانات الأجهزة تُحفظ على السيرفر (مصدر الحقيقة).

- [ ] التحقق من الـ endpoints في `mobile_api.py`:
  - `GET/POST /network/links` (قائمة/إضافة)
  - `PUT/DELETE /network/links/{id}` (تعديل/حذف)
  - `GET /signal-board` (لوحة الإشارة)
  - `GET /customers/{id}/signal` (إشارة الزبون)
  - `POST /network/ping` (فحص عبر السيرفر كبديل)
- [ ] إضافة أي endpoint ناقص (مثل `/network/sync` لسحب مشتركين الراوتر على السيرفر).
- [ ] ربط شاشة الشبكة بكل هذه الـ endpoints — الخلفية تعرض وتخزن، والفحص نفسه محلي.
- [ ] اختبار API من داخل التطبيق.

**التسليم:** 📦 سكتوراتك تُحفظ على السيرفر وتظهر في التطبيق والويب معاً.

**البوابة:** تضيف سكتور من التلفون → تظهر في الويب → موافقة → المرحلة 3.

---

## المرحلة 3 — السرعة والجمال (روح Blood)
**الهدف:** التطبيق سريع وحلو مثل استايل Blood.

- [ ] تحميل فوري: قراءة من Room cache أولاً ثم تحديث من السيرفر (Cache-First).
- [ ] حالات عربية واضحة: تحميل/فارغ/خطأ + سحب للتحديث.
- [ ] ضبط RTL + الأرقام العربية (٠-٩) لكل القيم.
- [ ] تحسين الشكل: أزرار كبيرة، كروت نظيفة، تبويبات واضحة.
- [ ] رسائل نجاح/خطأ عربية أنيقة (Toast).

**التسليم:** 📦 التطبيق سريع وحلو، عربي كامل، بنفس سلاسة Blood.

**البوابة:** انطباعك الشخصي → موافقة → المرحلة 4.

---

## المرحلة 4 — التكامل الشامل (Frontend ↔ Backend ↔ Database)
**الهدف:** لا انكسار، كل شيء متطابق.

- [ ] مراجعة `API_CONTRACT.md` ومطابقة كل الـ endpoints.
- [ ] تشغيل الاختبارات: `test_mobile_api.py` + `test_db.py` + `test_launcher_flow.py` + `test_auth_rbac.py`.
- [ ] إصلاح أي فشل.
- [ ] اختبار مسارات كاملة من التطبيق (Login → Dashboard → أجهزة → فحص).

**التسليم:** 📦 صفر روابط مكسورة — كل API يستجيب بشكل صحيح.

**البوابة:** تشغيل الاختبارات أمامك أو تأكيدها → موافقة → المرحلة 5.

---

## المرحلة 5 — بناء APK وتسليمه
**الهدف:** ملف APK جاهز للتركيب.

- [ ] تشغيل `build_android.bat` → `android_native/app/build/outputs/apk/debug/app-debug.apk`.
- [ ] (اختياري) توقيع release APK: إنشاء keystore + توقيع عبر `apksigner`.
- [ ] توثيق خطوات التثبيت في `BUILD_APK.md` (تحديث).

**التسليم:** 📦 APK جاهز للتركيب على الهاتف.

**البوابة:** تستلم الـ APK → موافقة → المرحلة 6.

---

## المرحلة 6 — الفحص النهائي على جهاز حقيقي
**الهدف:** التأكد أن كل شيء يعمل على الأرض.

- [ ] تثبيت الـ APK على تلفونك.
- [ ] تجربة: تسجيل دخول + لوحة + زبائن + أجهزة.
- [ ] فحص Ping لهدف حقيقي.
- [ ] فحص سكتور حقيقي (TCP + RouterOS + SNMP).
- [ ] إضافة/تعديل سكتور → يظهر في الويب (الخلفية).
- [ ] موافقتك النهائية.

**التسليم:** 🏁 التطبيق مكتمل، يعمل، وموافق عليه.

---

## ✅ تعريف «مكتمل» (Definition of Done — عام)
- [ ] تطبيق أندرويد أصلي (Kotlin/Compose) سريع وحلو.
- [ ] الخلفية = السيرفر (Flask/Render) — كل البيانات تُحفظ هناك.
- [ ] قسم الأجهزة يعمل محلياً من التلفون: Ping + Sector + SNMP.
- [ ] تطابق تام بين Frontend ↔ Backend ↔ Database.
- [ ] عربي RTL كامل + أرقام عربية.
- [ ] `build_android.bat` يبني APK أخضر.

---

## ⏱️ التقدير الزمني
| المرحلة | الوقت التقريبي |
|---|---|
| 0 — تحضير | ساعة |
| 1 — قسم الأجهزة | يوم |
| 2 — ربط الخلفية | 3–4 ساعات |
| 3 — السرعة والجمال | 3–4 ساعات |
| 4 — التكامل | 3–4 ساعات |
| 5 — بناء APK | ساعة |
| 6 — فحص نهائي | حسب جهازك |

---

**🧭 الحالة الحالية:** ✅ المرحلة 0 جاهزة للتنفيذ — تشغيل `build_android.bat` للتحقق من سلامة البناء قبل أي تعديل.