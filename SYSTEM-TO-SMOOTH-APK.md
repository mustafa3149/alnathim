# SYSTEM → SMOOTH APK
## تحويل أي نظام إلى تطبيق أندرويد سلس (مثل تطبيق الدم 🩸)

> هذا الدليل يشرح كيف تحوّل **أي نظام** بنيته (موقع / Flask / سطح مكتب) إلى **تطبيق أندرويد سريع وسلس** —
> صفحة واحدة، تنقّل فوري، والخادم يعمل في الخلفية.
> مكتوب ليفهمه الإنسان **والذكاء الاصطناعي** معاً، ليطبّق نفس الأسلوب في أي مشروع.

---

## 1) الهدف / The Goal

- يفتح المستخدم التطبيق على هاتفه → كل شيء يظهر **فوراً** (بدون انتظار، بدون "جارِ التحميل...").
- التنقل بين الأقسام **فوري** — مثل تطبيق الدم: **صفحة واحدة فيها كل الأقسام**.
- الخادم (السيرفر) يقوم بالعمل الحقيقي في الخلفية (الحسابات، الفواتير، التقارير).
- نفس النظام يعمل على: **الهاتف (APK) + الويب + الكمبيوتر (EXE)** — بنفس البيانات وبنفس المنطق.

---

## 2) المعمارية / The Architecture

```
[ تطبيق الهاتف (APK) ]  ←── JSON API ──→  [ خادم السحابة (Render) ]
    صفحة واحدة SPA         طلبات في         نفس برنامجك + قاعدة البيانات
    تعرض فوراً              الخلفية         يعمل كل العمل الحقيقي
```

- **الواجهة (Frontend)**: صفحة `index.html` واحدة تحتوي كل الأقسام (SPA). الملفات **داخل التطبيق** (Capacitor) فتفتح فوراً.
- **الخادم (Backend)**: نفس برنامجك يعمل على سحابة (Render). يعالج البيانات ويُرجع JSON.
- **التواصل**: التطبيق يرسل طلبات `fetch` إلى `/api/mobile/v1/...` فيردّ الخادم بالبيانات.

---

## 3) لماذا يشعر المستخدم بالسلاسة؟ / Why it feels smooth

| التقنية | الوظيفة |
|---|---|
| صفحة واحدة (SPA) | كل الأقسام في الذاكرة → التنقل فوري بدون إعادة تحميل |
| Cache-first | آخر البيانات محفوظة على الجهاز → تظهر فوراً ثم تُحدَّث بصمت |
| ملفات محلية داخل APK | التصميم يفتح فوراً حتى بدون إنترنت |
| Keep-alive | التطبيق يبقي السيرفر مستيقظاً (ping كل 10 دقائق) |
| Retry | إذا كان السيرفر يستيقظ، التطبيق يعيد المحاولة بصمت (لا أخطاء) |
| الخادم يحسب | الهاتف يعرض فقط → يبقى سريعاً مهما كبرت البيانات |

---

## 4) الخطوات / Steps (to convert ANY system)

### الخطوة 1: انقل الخادم إلى سحابة (Render)
- شغّل برنامجك (Flask / FastAPI / Express) على Render.
- أضف **JSON API endpoints** لكل عملية: إضافة / تعديل / حذف / قراءة.
- استخدم **غلاف استجابة موحد** دائماً:
```json
{ "ok": true,  "data": { ... } }
{ "ok": false, "error": { "code": "...", "message_ar": "رسالة عربية" } }
```
- الحماية: تسجيل دخول + رمز (Token) + صلاحيات (admin / agent).

### الخطوة 2: ابنِ الواجهة صفحة واحدة (SPA)
- ملف `index.html` واحد فيه كل الأقسام `<section class="view">`.
- شريط تنقّل سفلي (Bottom nav) يبدّل الأقسام عبر JavaScript — **لا روابط صفحات**.
- توجيه عبر الهاش (`#/view/customers`) حتى يعمل زر الرجوع وأزرار أندرويد.

### الخطوة 3: اربطها بالخادم
- دالة `api(path, body, method)` واحدة ترسل الطلبات مع Token وتعطي `{ok, data}`.
- اعرض القوائم فوراً ثم حدّث من الخادم في الخلفية (لا تنتظر).

### الخطوة 4: أضف طبقة السرعة (مثل تطبيق الدم)
- **localStorage cache**: احفظ آخر بيانات تحت مفتاح مثل `alnathim_cache_...`.
- **silentLoad(key, path, render)**: اعرض من الكاش فوراً → ثم fetch → حدّث بصمت.
- **keep-alive**: ping كل 10 دقائق حتى لا ينام السيرفر المجاني.
- **fetchWithRetry**: أعد المحاولة 2-3 مرات عند برودة السيرفر.

### الخطوة 5: غلّفها في APK (Capacitor)
- ملف `capacitor.config.json`:
```json
{ "appId": "com.your.app", "appName": "اسم التطبيق", "webDir": "frontend" }
```
- أوامر البناء:
```
npx cap add android
npx cap sync android
cd android
# للبناء السريع:
gradlew assembleDebug
# لبناء كامل نظيف (مضمون):
gradlew clean assembleDebug
```
- الناتج: `android/app/build/outputs/apk/debug/app-debug.apk`.
- أنسخه لمجلد جاهز باسم عربي للتثبيت على الهاتف.

### الخطوة 6: تحقق قبل التسليم
- `node --check app.js spa.js` → لا أخطاء صياغة.
- تأكد أن كل المعرّفات (IDs) المستخدمة في JS موجودة في HTML.
- **اقرأ الكود من داخل ملف الـ APK** (Zip) للتأكد أن التحديث دخل فعلاً.

---

## 5) قائمة التحقق الإلزامية / Mandatory Checklist

- [ ] صفحة واحدة SPA (لا إعادة تحميل عند التنقل)
- [ ] JSON API على سحابة (Render) يعمل كل العمل
- [ ] تسجيل دخول + صلاحيات (المدير يرى كل شيء، الوكيل جزء)
- [ ] Cache-first: البيانات تظهر فوراً من الجهاز ثم تتحدث بصمت
- [ ] Keep-alive يبقي السيرفر مستيقظاً
- [ ] Retry عند البرودة (لا أخطاء ظاهرة للمستخدم)
- [ ] زر الرجوع يعمل (hash routing)
- [ ] رسائل عربية واضحة لكل خطأ
- [ ] شاشات إدارة كاملة للمدير (إضافة/تعديل/حذف)
- [ ] APK يُبنى بدون أخطاء ويحتوي أحدث الكود

---

## 6) أفضل الممارسات / Best Practices

- **لا تعيد كتابة الواجهة لكل نظام** — استخدم نفس نمط SPA ثم غيّر الأقسام والبيانات.
- **الخادم يحسب، الهاتف يعرض** — الفواتير/التقارير/المطابقة على السيرفر، لا على الهاتف.
- **معرّفات ثابتة** لكل عنصر (IDs) حتى لا تنكسر الصفحات عند التحديث.
- **إخفاء أزرار المدير** عن غير المدير: افحص `role` من بيانات المستخدم وأخفِ `.admin-only`.
- **بيانات الهاتف حساسة**: لا تشارك الموقع الدقيق، والتواصل النهائي عبر واتساب.
- **النسخ الاحتياطي**: زر واحد على الشاشة يستدعي `/backup` على الخادم.
- **البناء النظيف** `gradlew clean assembleDebug` قبل كل تسليم لضمان أحدث الكود.
- **تحقق من داخل الـ APK**: افتحه كملف Zip واقرأ `assets/public/...` للتأكد.

---

## 7) مرجع حي من مشروعنا / Live reference (الناظم)

| الملف | الوظيفة |
|---|---|
| `mobile_app/index.html` | صفحة واحدة فيها كل الأقسام (SPA) |
| `mobile_app/js/spa.js` | المتحكم: التنقل + التحميل + الكاش + صلاحيات المدير |
| `mobile_app/js/app.js` | دوال مساعدة: api / toast / تنسيق / كاش / keep-alive |
| `mobile_api.py` | واجهة JSON API (كل العمليات مع الصلاحيات) |
| `capacitor.config.json` | إعدادات التغليف (appId / appName / webDir) |
| `android/` | مشروع أندرويد الناتج → يُبنى منه APK |

### خلاصة / Summary

**الوصفة الكاملة:**
`نظامك الحالي` + `JSON API على Render` + `صفحة واحدة SPA` + `طبقة سرعة (كاش + keep-alive + retry)` + `Capacitor` = `تطبيق أندرويد سلس مثل الدم` 🩸⚡