"""FastAPI REST gateway package (Phase 4).

Importing this package stays light — the FastAPI app lives in
api_gateway.main so heavy third-party imports happen only when the
server is actually launched.
"""